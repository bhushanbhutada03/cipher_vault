# PROJECT RULES

Backend is frozen.

Never modify backend.

Never regenerate frontend.

Never create another React project.

Continue only inside:

frontend/secure-credential-organizer-ui

Never rename DTOs.

Never rename endpoints.

Never invent APIs.

Never change request or response fields.

Always use existing architecture.

Every page must:

- compile
- pass build
- pass lint

Always stop after completing one page.

Wait for review.